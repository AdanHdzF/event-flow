version: "3.9"

services:
  app:
    image: eventflow:latest
    container_name: eventflow-app
    ports:
      - "8080:8080"
    environment:
      - KAFKA_BOOTSTRAP_SERVERS=kafka:9092
      - RABBITMQ_HOST=rabbitmq
      - RABBITMQ_PORT=5672
      - RABBITMQ_USERNAME=user
      - RABBITMQ_PASSWORD=password
    depends_on:
      kafka:
        condition: service_healthy
    #   rabbitmq:
    #     condition: service_healthy

  kafka:
    image: apache/kafka:3.9.0
    container_name: eventflow-kafka
    ports:
      - "9092:9092"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:29093
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      #Listeners
      KAFKA_LISTENERS: PLAINTEXT://kafka:9092,CONTROLLER://kafka:29093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,CONTROLLER:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT

      #Cluster
      CLUSTER_ID: "eventflow-kafka-cluster-001"
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1

      #Log retetion
      KAFKA_LOG_RETENTION_HOURS: 168
      KAFKA_LOG_SEGMENT_BYTES: 1073741824

    volumes:
      - kafka_data:/var/lib/kafka/data

    healthcheck:
      test: /opt/kafka/bin/kafka-topics.sh --bootstrap-server kafka:9092 --list
      interval: 10s
      timeout: 10s
      retries: 10
      start_period: 30s

  # rabbitmq:
  #   image: rabbitmq:4.1-management
  #   container_name: eventflow-rabbitmq
  #   ports:
  #     - "5672:5672"
  #     - "15672:15672"
  #   environment:
  #     RABBITMQ_DEFAULT_USER: user
  #     RABBITMQ_DEFAULT_PASS: password
  #   volumes:
  #     - rabbitmq-data:/var/lib/rabbitmq
  #   healthcheck:
  #     test: rabbitmq-diagnostics -q ping
  #     interval: 10s
  #     timeout: 10s
  #     retries: 10
  #     start_period: 20s

  # sqlserver:
  #   image: mcr.microsoft.com/mssql/server:2022-latest
  #   container_name: eventflow-sqlserver
  #   ports:
  #     - "1433:1433"
  #   environment:
  #     ACCEPT_EULA: "Y"
  #     MSSQL_SA_PASSWORD: "EventFlow#2025"
  #     MSSQL_PID: Developer
  #   volumes:
  #     - sqlserver-data:/var/opt/mssql
  #   healthcheck:
  #     test: /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "EventFlow#2025" -C -Q "SELECT 1" -b
  #     interval: 10s
  #     timeout: 10s
  #     retries: 10
  #     start_period: 30s

  # opensearch:
  #   image: opensearchproject/opensearch:2.19.1
  #   container_name: eventflow-opensearch
  #   ports:
  #     - "9200:9200"
  #     - "9600:9600"
  #   environment:
  #     discovery.type: single-node
  #     DISABLE_SECURITY_PLUGIN: "true"
  #     OPENSEARCH_JAVA_OPTS: "-Xms512m -Xmx512m"
  #     bootstrap.memory_lock: "true"
  #   ulimits:
  #     memlock:
  #       soft: -1
  #       hard: -1
  #     nofile:
  #       soft: 65536
  #       hard: 65536
  #   volumes:
  #     - opensearch-data:/usr/share/opensearch/data
  #   healthcheck:
  #     test: curl -s http://localhost:9200/_cluster/health | grep -q '"status":"green"\|"status":"yellow"'
  #     interval: 10s
  #     timeout: 10s
  #     retries: 10
  #     start_period: 30s

  # opensearch-dashboards:
  #   image: opensearchproject/opensearch-dashboards:2.19.1
  #   container_name: eventflow-opensearch-dashboards
  #   ports:
  #     - "5601:5601"
  #   environment:
  #     OPENSEARCH_HOSTS: '["http://opensearch:9200"]'
  #     DISABLE_SECURITY_DASHBOARDS_PLUGIN: "true"
  #   depends_on:
  #     opensearch:
  #       condition: service_healthy
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:5601"]
  #     interval: 10s
  #     timeout: 10s
  #     retries: 10
  #     start_period: 30s

volumes:
  kafka_data:
# rabbitmq-data:
# sqlserver-data:
# opensearch-data:
